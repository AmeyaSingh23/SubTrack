// app/page.tsx
// This is the homepage ("/"). For now it's a placeholder.
// We'll build the real landing page after auth is set up.

export default function HomePage() {
  return (
    <main>
      <h1>SubTrack is alive.</h1>
    </main>
  );
}